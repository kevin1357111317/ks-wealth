# KS Wealth 版號規則

目前正式版：**V3P1**

## 命名

- **大更新**：`V1 -> V2 -> V3`
- **小更新**：`V1 -> V1P1 -> V1P2 -> V1P3 -> V1P4 -> V1P5`
- 大更新時，小版號歸零：例如 `V1P8 -> V2`

## 什麼算大更新

出現下列任一情況，原則上升大版：

- 新增主要功能頁、主要分析模組或新的核心使用流程
- 大幅改版 UI / 導航 / 資訊架構
- 資料模型或核心財務計算邏輯有重大變更
- 多個相關功能一次整批重做

## 什麼算小更新

以下通常升小版：

- UI 排版、文案、顯示欄位調整
- bug fix
- 資料校正
- 報價、效能、快取優化
- 小功能或既有功能的局部增強

## 出貨規則

每次要推正式環境前都要先決定這次是大更新或小更新，然後：

1. 修改 `app-version.js` 裡的 `APP_VERSION`。
2. 同步更新本文件的「目前正式版」。
3. 修改 `index.html` 中 `/app-version.js?v=` 的快取字串為新版本。
4. commit message 建議帶版號，例如：`V1P1：修正貸款卡片展開動畫`。

`app-version.js` 是畫面顯示版號的唯一來源；App 頂端會自動顯示目前正式版。
