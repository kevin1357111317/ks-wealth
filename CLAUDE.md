# KS Wealth 工作方式

所有協作者先讀 `AGENTS.md`；本檔保留專案的完整實作與出貨細節。

屋主是鎧麟，工程背景。互動偏好寫在下面「跟鎧麟對話的方式」，那一節的優先度高於本檔其他
風格描述；專案規則仍以技術章節為準。

## 跟鎧麟對話的方式

長期偏好，除非當次另有指定就照這個走。

### 結論先講

順序是**結論 → 為什麼 → 數據／比較 → 怎麼做**。不要先鋪一段背景知識才講答案。
問「A 跟 B 哪個好」就先回「以你的情況我選 A」，再解釋；不要列完優缺點叫他自己選。

### 要下判斷，不要安全牌

資訊夠就直接判斷：「我選 A，理由有三個。」
不確定就講機率與變數：「目前資訊下大概 70% 選 A，最大變數是 X。」
這比「各有優缺點、取決於你的需求」有用得多。

不要為了迎合他而附和。他的邏輯有漏洞就直接說，有更好的第三個選項也主動提，不必被他給的
A／B 侷限。指出他理解錯的地方可以直說「這邊你的理解有點反了，實際上是……」。

### 語氣

台灣繁體中文，自然口語，不要像客服、論文或新聞稿。

少用：「這是一個很好的問題」「你的想法非常棒」「讓我們一步一步來」、反覆出現的
「需要注意的是」、過度客套、過度安慰、大量免責聲明。

### 比較題用表格

A vs B、哪個划算、差多少這類問題盡量用表格，只留會影響決策的欄位，表格後面一定補一句
明確結論：「所以以你的用途我推薦 X。」差異很小也要直說：「這兩個差很少，不值得為它多花 XX。」

### 數字要真的算出來

投資、貸款、報酬率、價差、百分比、成本、距離、時間、機率 —— 全部實際算出數字，不要只給公式。
不要說「油耗可以用油價除以平均油耗算」，要說「95 無鉛 30.5 元/L、14 km/L，大約 2.18 元/km」。
公式必要時再附。

### 需要即時性就去查

價格、股票、ETF 成分、店家、商品規格、車價、優惠、活動、法規、版本、最新消息、近期網友評價 ——
不要只靠模型既有知識。他說「你查一下 / 上網看 / PTT 呢 / 現在多少 / 最近成交價」就是要查最新的。
注意資料日期，不要拿幾年前的文章當現況。

問社群評價（PTT、Dcard、Mobile01、Reddit、Threads）時要整理出反覆出現的共識，並區分
普遍共識／少數案例／主觀偏好／可客觀驗證的問題，不是「有人喜歡有人不喜歡」。

### 不要一直問他問題

資訊夠就直接做最佳版本，不要每步都問「要不要我……」「是否需要……」。
只有缺了會讓答案完全不同的關鍵資訊才問，而且**前面講過的不要再問第二次**。

### 記住前文

「那這個呢」「所以？」「為啥？」「再比較一次」「按照你的建議修」「不是」「我剛剛不是說……」
都是延續前文的追問，不要從零重答，也不要要求他重新描述完整問題。
他糾正過一次之後，後面所有推論都用新版本，不要又跳回舊的。

他說「不是／不對／你搞錯了」時，不要花篇幅替原答案辯護，先確認「懂了，你指的是 X 不是 Y」再繼續。

### 技術與改專案

工程背景，API、DB、SQL、Supabase、RLS、Vercel、deployment、migration 這些直接談，不用當初學者解釋。
問「為什麼」才深入原理。

改既有專案一律 **incremental patch**：能改局部就不要重寫，不要為了一個 UI 小問題重生整份程式
或動到正常運作的功能。順序是 root cause → 確認影響範圍 → 最小修改 → 跑測試 → 確認沒有 regression
→ 才發佈。發現其他問題可以一併指出，但不要擅自大改架構。

這裡的 **incremental patch 是開發手法，不是版本等級**。SemVer 的 PATCH／MINOR／MAJOR 必須依
`VERSIONING.md` 用相容性判斷；小 diff 也可能是 breaking change，大 refactor 也可能完全不需要 MAJOR。

已經上線的行為是基準：資料邏輯、計算方法、UI 結構、命名、既有功能，沒有明講要 redesign 就不要動。

Debug 時要排序可能性：「從症狀看我第一個懷疑 A，因為 X；B 第二；C 機率很低」，並給最短的驗證方式，
不要只丟「可能是 A、B、C」。

### 其他領域

- **投資**：不用一直提醒有風險，他知道。篇幅放在估值、成長、成分、集中度、產業曝險、
  報酬／風險、機會成本、ETF 差異、他的組合會怎麼變。資訊夠就直接給配置建議。
- **健康**：把危險程度分級講清楚 —— 很常見可觀察／建議安排門診／建議近期檢查／需要立即就醫。
  不要看到症狀就一律叫他去醫院，也不要淡化真正有風險的。重點是「什麼可以觀察、什麼不能再等」。
- **旅遊**：不是一天塞最多景點。看的是順不順、交通合不合理、不要太早起、不要趕路、實際步行距離、
  停留時間、車班銜接、吃飯時間、值不值得去。排走得完的行程；不值得繞路的景點直接說。

### 長度

**預設一律短答、白話。** 就算問題本身很複雜、就算是 code review 或除錯，也先給結論加最關鍵的
一兩點，不要預設攤開成長篇報告。三句話能講完的不要寫成一千五百字。

只有他明講「全部分析／詳細比較／深入一點／完整 review／講細一點」時才完整攤開。
想講的細節先留著，他要再給 —— 不要因為「這題很重要」就自己決定寫長。

用詞也要白話：能用日常講法就不要堆術語，非用不可的技術名詞順手用一句話解釋。

他習慣逐層追問（A 是什麼 → 為什麼 → 那 B 呢 → 所以選哪個），先回答當下那一題就好，
保留脈絡讓他繼續問，不要第一題就把教科書倒完。

## 這個專案

家庭資產 App，正式站 https://ks-bubu-yier.vercel.app 。純前端（`app-v3.js` 一支扛全部）
+ Supabase（Postgres／RLS／Edge Functions）。沒有打包步驟 —— 改完 push 到 `main`，
Vercel 直接部署靜態檔。

**改前端一定要記得動 `index.html` 裡的 `?v=` 快取字串**，不然 PWA 會續用舊檔。

`README.md` 是真正的設計文件：每個怪地方為什麼長這樣、數字跟哪份資料對過，都寫在裡面。
動到某一塊之前先讀那一節，改完把新的理由補回去。

## 版控

正式版號規則以 `VERSIONING.md` 為唯一判級依據，採 Semantic Versioning（SemVer）精神。

目前 `V3P26` 是 legacy 最後一版；歷史不回寫。下一次產品出貨起使用 `VMAJOR.MINOR.PATCH`：

- breaking change → **MAJOR**，例如 `V3.26.0 -> V4.0.0`
- 向後相容新功能 → **MINOR**，例如 `V3.26.0 -> V3.27.0`
- 向後相容 bug fix → **PATCH**，例如 `V3.26.0 -> V3.26.1`
- 純 docs / test / CI / 協作規則且不改正式產品 → **none**，不升產品版號

每個 PR 合併前都必須寫明 `Version impact: none / patch / minor / major` 與理由；同一 PR 有多種變更時取
最高等級。**不能因為這次是 incremental patch 就自動判成 PATCH，也不能因為新增一頁就自動判成 MAJOR。**
相容的新功能照 SemVer 是 MINOR；只有破壞既有資料、計算語意、client contract 或操作相容性才是 MAJOR。

產品正式出貨時，`app-version.js` 裡的 `APP_VERSION` 是畫面顯示版號的唯一來源，並同步更新：

1. `app-version.js` 的 `APP_VERSION`
2. `VERSIONING.md` 的目前正式版
3. `index.html` 的 `/app-version.js?v=` 快取字串

新提交建議採 Conventional Commits：`fix:` 通常對應 PATCH、`feat:` 通常對應 MINOR、`!` 或
`BREAKING CHANGE:` 對應 MAJOR；但最終仍以實際相容性為準。

## 出貨流程

從最新 `main` 建立各自的工作分支，開 PR，squash merge 進 `main`。

`node --test tests/*.test.mjs` 要全過才推。playwright 沒裝的話測試會自己 skip，
先 `npm i --no-save playwright`（Chromium 已經在 `/opt/pw-browsers`，不要再下載）。
平行跑偶爾會因為同時開太多 Chromium 逾時，`--test-concurrency=1` 可以確認是不是假紅。

改到會動錢的邏輯，測試要先確認「拿掉修正就會紅」再算數 —— 這個 repo 裡已經有兩次
測試對著沒修好的程式碼變綠的紀錄。

**推上去之後要等 CI 跑完再回報**，不要推完就說「做好了，CI 綠了再跟你說」。
一輪 CI 大約 1～2 分鐘，等它跑完再講，屋主才不用自己去看、也不會先收到一個之後被推翻的結論。
紅了就先查、修完再回報，不要把紅燈丟回去給他。

#### 等 CI 要用會自己結束的 until 迴圈，不要用背景 sleep

2026-09-21（#173）與 09-22（#176）兩次都誤判成「CI 卡住二十幾分鐘」，其實那兩個 job
分別只跑了 75 秒與 82 秒。**問題出在等待方式，不是 GitHub。**

`sleep` 丟到背景（`run_in_background`）**不會擋住自己的回合** —— 它立刻回傳，然後就
接著往下戳下一次查詢。所以以為「等了 3 分鐘再查」，實際只過了十幾秒；連戳五、六次還
看到 `in_progress`，就誤以為是狀態 API 在回快取。實測容器時鐘與 GitHub 伺服器時鐘
完全一致，兩邊的狀態端點也都如實回報，沒有快取問題。

正確做法是跑一個**條件成立才結束**的背景指令，這樣只會在真正跑完時收到一次通知：

```bash
until curl -sS -H "Authorization: Bearer ${GH_TOKEN:-$GITHUB_TOKEN}" \
        -H "Accept: application/vnd.github+json" \
        "https://api.github.com/repos/kevin1357111317/ks-wealth/actions/runs/<run_id>/jobs" \
      | python3 -c "import json,sys; sys.exit(0 if all(j['status']=='completed' for j in json.load(sys.stdin)['jobs']) else 1)"
do sleep 20; done
```

這裡用 `curl` 而不是 GitHub MCP，單純因為 **MCP 工具沒辦法在 shell 迴圈裡呼叫**。
這台機器連得到 `api.github.com`，環境變數本來就有 `GH_TOKEN`／`GITHUB_TOKEN`，一次
就拿到 job 的 `status`／`conclusion` 與每個 step 的結論。迴圈結束後再看結論即可；
單次查看用 MCP 或 curl 都行。

（注意 proxy 會攔截 `https://api.github.com/` 根路徑並回一個假的 200 空回應，
拿它測連通性會被騙。要測就打真正會回資料的 endpoint。）

**CI 綠了就直接 squash merge，不用再問一次**。他要的東西做完、測試過了、CI 也綠了，
再回頭問「要 merge 嗎」只是多一趟來回。merge 完把結果連同後續動作（部署、驗證）一次講完。

例外，這幾種還是要先問：CI 紅、有 merge conflict、判到 `major`、動到金融數字或計算定義、
或者實作過程中發現跟他原本要的不一樣。

### Edge Function 要自己部署

`supabase/functions/` 底下的 Edge Function **不在 Vercel 的部署範圍**。推 `main` 只會部署前端靜態檔；
`portfolio-performance` 這種真正在算 TWR／XIRR 的程式必須另外部署，否則會出現「畫面版號是新的、
數字卻是舊的」這種半套狀態。

2026-09-18 就踩過一次：前端 V3.34.1 已上線、圖表日期也帶年份了，但累積 TWR 還是 205%、2020 年那根
假尖峰還在，因為 Edge Function 停在舊版。當時只驗了 `app-version.js` 回新版號就宣布上線，而那只證明
前端上線了。

改到 `supabase/functions/` 的出貨步驟：

1. squash merge 進 `main`。
2. 部署那支 function（`supabase functions deploy <name>`，或 Supabase MCP 的 deploy 工具）。
3. 撈回線上原始碼跟 repo 逐字比對，確認部署到的是合併後的版本。
4. **驗數字，不是驗版號** —— 打開實際頁面，確認預期會變的指標真的變了。

目前的 function：`portfolio-performance`（TWR／XIRR）、`daily-wealth-snapshot`、`refresh-tw-quotes`。

## 資料

貸款、股票私帳、美金這幾塊的數字都跟 KLFAN 試算表或銀行 App 核對過，`README.md` 裡有
對照結果。動到計算邏輯就重新對一次，別只看測試綠。

## 私帳對帳

**對帳單為準，私帳是副本。** 只要拿得到券商對帳單（國泰證券 App 可自行匯出 CSV；其他家要跟
營業員申請），那段期間就以對帳單為準，私帳直接覆寫成對帳單的樣子 —— 日期、金額、股數、
連「幾筆成交」都要一致。私帳是人工輸入的副本，對不上就是副本錯。

### 方法

先做 **per-(標的, 日期) 彙總比對**找出有問題的日子，再對那幾天做逐筆比對。不要一開始就逐筆比，
會被下面第一個眉角騙。

比完要能把每一處差異歸到五類之一：**日期錯／金額錯／拆併不同／整筆漏記／券商標錯**。
歸不了類就是還沒查清楚，不要動資料。

**配對鍵不要放券商。** 券商欄位本身就是會錯的欄位之一（見坑 5）。先用
`(標的, 日期, 種類, 股數, 金額)` 配對，配上之後再回頭看券商標得對不對。

### 收尾一定要跑逐筆雙向配對

改完之後不能只說「這段對完了」，要把對帳單與私帳各自攤成
`(標的, 日期, 種類, 股數, 金額)` 的多重集合，做 full outer join，**列出兩邊筆數不相等的每一組**。
兩個方向都要列：對帳單有而私帳沒有（漏記），私帳有而對帳單沒有（多記或不屬於這家）。

**「日期區間有被對帳單涵蓋」不等於「已驗」。** 同一段期間裡會混著別家券商的交易、信託扣款、
以及對帳單根本不列的股息 —— 它們日期落在區間內，但那份對帳單本來就不該有它們。用區間算覆蓋率
會把這些全部算成已驗。2026-09 就是這樣把兩筆元大的交易誤報成「國泰已驗、100% 對完」，
是逐筆配對才抓出來的。覆蓋率的分母是**配對得上的筆數**，不是落在區間內的筆數。

### 沒有券商對帳單時，用銀行帳戶的交割金額回推

券商調不到對帳單（國泰美股就匯不出來）不等於沒得驗。國泰世華**外幣存款明細**
（帳號 074-08-…）裡的「證清轉 交割股款」是每日淨額，「證清轉 VOO 息」這種還直接帶標的，
足以把整本美股私帳驗完；台幣存款明細（074-50-…）同理，裡面的「台積電 現金股息」可以驗股息入帳日。

這種來源只有**每日淨額**，沒有逐筆，所以方法跟逐筆對帳單不一樣：

1. **先驗 parse**：把每一列的餘額串起來，逐日餘額必須首尾相接、零斷點，才代表 PDF 沒掉行。
   （注意排序：跨日是新到舊，但同一天之內是舊到新，直接拿相鄰兩列相減會假性斷鏈。）
2. **把私帳推成交割日**，再跟每日淨額比。逐日比會滿江紅 —— T+1／T+2 差一天就整天對不上。
3. **看累積差額的「平台」，不要看單日差額。** 把 Σ私帳 − Σ對帳單 逐日累積起來畫出來：
   時間差只會讓它跳一下就跳回原位，真正的差異才會讓它**換到新的平台高度**。
   平台換階的金額就是那筆差異的金額，階數就是差異的筆數。
   2026-09 用這招把 717 筆美股交易收斂成 10 個階，其餘 700 多筆一毛不差。
4. **再用 subset-sum 逐日還原**：對每個交割日，從 T-1～T-4 的未配對交易裡湊出剛好等於當日淨額的
   子集合。湊得出來就是那天結清了；湊不出來的日子，加上上面的平台分析，就能把差異釘到單筆。
5. 只知道金額、不知道標的時，用 `金額 ÷ 1.001 ÷ 當日收盤價` 反推股數 —— 整數股就是命中。
   2024-05-29 那筆漏記的買進就是這樣認出來是 NVDA 50 股（5,746.99 / 1.001 / 114.83 = 50.00000）。

**歸不到單一筆的金額差不要動。** 每日淨額推不出「是哪一列寫錯」，硬改就是猜。留著、寫清楚金額，
等真的拿到券商對帳單再修。

**更重要：不要用「哪一筆差額剛好等於殘差」去反推。** 2026-09 用銀行淨額算出 8.87 的殘差，
剛好有一筆 NVDA 的私帳金額比推估值少 8.87，就把它改掉了 —— 結果拿到逐筆對帳單才發現那筆
NVDA 本來就是對的，真正的 8.87 來自一檔私帳完全沒有的 GOOGL 當沖（買 −13,700.15、賣 +13,709.02）。
**殘差的來源必須在對帳單上真的看到那一列**，金額吻合只是必要條件，不是充分條件。

### 逐筆對帳單：三件會讓「照抄」出錯的事

1. **對帳單裡有不是交易的列。** 2024-06-10 NVDA 同日兩列、金額都是 12,051.44、方向相反，
   股數 110 對 11（比例剛好 10）、手續費都是 0 —— 那是十股換一股分割的帳務沖銷，不是買賣。
   照抄會把股數灌爆。偵測方式：同標的同日、金額等值反向、股數比等於分割比、手續費為 0。
2. **對帳單會出現私帳整檔沒有的股票。** 這次抓到 NVDL 與 GOOGL，兩檔都是當沖，所以持股、
   期末市值完全看不出來。**掃代號時不要套白名單** —— 用白名單做模糊比對會把 GOOGL 硬配成
   VOO／GLW，反而把「這檔不存在」這個事實藏起來。
3. **同一天的成交會被跨頁切斷。** 2024-09-19 的 TSM 有 4 筆，前兩筆在上一頁、後兩筆在下一頁；
   只看一頁會少算，然後誤判成金額不符。

### 掃描版對帳單的 OCR 做法

國泰調出來的是 **200 dpi、1-bit 傳真品質**的掃描檔，小數點常常整個不見。有效的順序：

1. **從 PDF 裡的原生點陣圖處理**，不要先 render 成 300 dpi 再處理 —— 那只是把 1-bit 影像
   重採樣，不會多出資訊，反而讓筆畫糊掉。
2. **先 deskew**。整頁歪 1 度，逐欄裁切就會整欄錯位。
3. **每頁單獨做水平對齊**：各頁掃描位移最多差 60 px，欄位邊界寫死會讓數字卡在邊界被截斷。
   用每列都有的 `USD` 欄當錨點算出位移量再套用。
4. **逐欄裁切 + 數字白名單**辨識，不要整頁一次 OCR。
5. **小數位數是固定的**（股數／成交金額 6 位、價格 4 位、金額 2 位），掉了小數點可以補回去。
6. **最後用「群組合計」當約束修復**：每個 (標的, 日期) 的合計已經跟銀行對過是對的，
   逐筆金額加起來必須等於它。這一步會自動修掉個別數字的辨識錯誤，也是唯一能讓
   「用 OCR 的結果去改金融資料」安全的理由 —— 沒有這個約束就不要改。

**不是每份掃描檔都值得 OCR。** 中信調出來的是 300 dpi 灰階，肉眼直接判讀又快又準；
同一套 OCR 流程套上去反而一直吃掉逗號跟首位數字。先看原生點陣圖的 dpi 與色深再決定，
`uniq 256`（灰階）通常直接讀比較省事，1-bit 傳真才需要整套前處理。

### 報表的合計數是最強的驗證

券商正式報表通常在最後一頁印合計。中信的「分戶帳歷史查詢報表」一次給五個獨立約束：
**總買賣筆數、買入股數、買入應收、賣出股數、賣出應付**（淨應付可由後兩者導出）。
把自己抄出來的逐筆加總，五個全中才算轉錄正確 —— 單一筆抄錯不可能五個約束同時還吻合。
沒有這個檢查就不要拿抄出來的結果去改資料。

國泰的「客戶交易明細表」印的是 `交易筆數` 與手續費／交易稅／價金／淨收付合計，同樣可用。
**注意合計的筆數不含非交易列**（見下方坑 7），對不上先確認是不是把存券轉也算進去了。

### 掃描檔可能整段掉頁首

國泰那份台股 4 頁裡，第 3、4 頁進掃描機時整張往上偏，頁首連同前面幾列直接落在掃描範圍外 ——
頁面尺寸、頁數都正常，看起來完全不像缺資料，只有「日期突然跳過一段」會露餡。
偵測方式：**看每頁最後一列與下一頁第一列的日期是否連續**；連續頁之間跳掉一段就是掉列，
不是那段沒交易。缺的那幾列用另一個來源（券商 App 匯出的 CSV）補，不要當成「對帳單沒有」。

### 七個踩過的坑

1. **私帳會把多筆成交合併成一筆。** 對帳單 10 股分兩次成交，私帳常記成一筆 10 股。逐筆比會看到
   「對帳單有、私帳沒有」，誤判成漏單。**先看加總**：股數與金額加總相等就是拆併問題，不是漏單。
   現在的規定是拆回對帳單的原始筆數（見 `AGENTS.md`），拆完淨額不變，TWR／XIRR 也不該變 ——
   變了就是拆錯。
2. **要用對帳單的「交易日期」，不是「交割日期」。** 私帳有系統性的「早一天」毛病，月底月初特別多。
   股息那幾筆同樣會差一天。
3. **當沖的淨股數是 0，所以「看持股對不對」完全抓不到。** 整檔標的漏記也照樣對得起來，因為它
   從頭到尾不影響任何一天的持股。只有逐筆比對帳單會浮出來。已經這樣漏掉過好幾檔。
6. **不只當沖 —— 整段波段買賣一起漏記，持股一樣對得起來。** 2026-09 抓到 NVDA
   2024-05-29 買 50 股、2024-06-20 賣 50 股兩筆同時不在私帳裡，中間隔了三週。淨股數是 0，
   所以持股數、`financial_items`、期末市值全部照樣吻合，兩年多都沒人發現；只有銀行的交割金額
   會露餡。**「持股對得起來」永遠不能當成「交易記全了」的證據**，不管是不是當沖。
4. **金額慣例**：私帳記的是**實際現金流**，不是成交金額 ——
   **買進 = 成交金額 + 手續費**、**賣出 = 成交金額 − 手續費 − 交易徵費**。
   **不要用固定費率反推。** 這裡原本寫「賣出 × 99.9%、買進 × 100.1%」，那是 2024 年當時
   個股 0.1% 的費率，現在既不適用個股（已降到 0.08%）也從來不適用 ETF（是每筆均一價）。
   費率表見下方「國泰複委託美股的費用」。
   交割是買進 T+1、賣出 T+2，當沖會併成一筆淨額入帳，所以用存摺交割金額反推時要先併同日同結算。
5. **券商欄位會標錯，而且兩個方向都有。** 已經抓到多筆互標（A 家標成 B 家、B 家標成 A 家）。
   所以「這筆在甲家對帳單裡找不到」的第一個假設是**它屬於別家**，不是它不存在 —— 先去其他家的
   對帳單找，找到了就改標籤，不要刪。也因此同一檔標的可能同時存在於券商庫存與員工持股信託，
   那是兩個獨立的池子，私帳記在同一個標的下是對的。
7. **非交易列不只分割沖銷，還有「存券轉」。** 券商之間搬股票會在對帳單上留下
   `存券轉入／存券轉出` 的列：有股數、沒有單價也沒有金額。它不影響現金、也不影響總持股
   （私帳是按標的記帳、不分券商），照抄會把股數灌爆。報表的合計筆數本來就不含這些列，
   所以**筆數對不上時先確認有沒有把它們算進去**。

### 國泰複委託美股的費用

手動補單時用來驗「金額算得對不對」。以官網 `cathaysec.com.tw/CathaysecUS/us_transaction_info.aspx`
2026-09-22 的版本為準（那個網域被開發機的 egress proxy 擋住，要看得自己開）：

| 類型 | 買進手續費 | 賣出手續費 |
|---|---|---|
| **一般股票（網路單）** | 0.08%，**免收最低手續費** | 同左 |
| 一般股票（人工單） | 0.08%，**每筆最低 USD 10** | 同左 |
| **美股 ETF** | **每筆 USD 3**（與金額無關） | **每筆 USD 3** |
| 定期定股 | 0.08%，免收最低 | 股票比照一般股票；ETF 每筆 USD 3 |
| 定期定額 | 每筆均一價 USD 0.1 | 同上 |

0.08% 是**優惠價，到 2026/12/31**，之後要重新確認。

**交易徵費（SEC fee）只有賣出才收**，= 賣出金額 × **0.00206%**，看起來是無條件進位到分。
實測 2026-09-22 賣 VOO 成交金額 3,565.25 → 3,565.25 × 0.00206% = 0.0734，App 顯示
「其他費用 0.08」。官網註明實際依 SEC 公告為準，所以**以 App／對帳單上的數字為準，不要自己算**。

幾個會影響金額但容易忘的：

- **美股現金股利預扣 30%**。私帳的股息記的是**實際入帳金額（已扣稅）**，不是宣告股利。
  算殖利率時要記得這件事。
- 義大利／西班牙／法國在美國掛牌的 **ADR 另課 0.1%~0.3% 金融交易稅**。
- **ADR 存託銀行管理費**每股 USD 0.02~0.05，不定期收，會變成一筆莫名其妙的小額扣款。
- 公司活動（回購、公開收購、換股）若保管機構是 VIEWTRADE，每次 USD 50。

**ETF 每筆 3 美元是固定成本，所以小額分批很貴** —— 買 1 股 62.91 的手續費佔 4.77%，
買 56 股 3,536.39 只佔 0.085%。個股（0.08% 無低消）則完全沒有這個問題。

### 對帳單本身可能被截斷

匯出檔第一行常會寫「總計有 N 筆資料，當前資料為 1-50 筆」。**N 大於 50 就是截斷**，
不能當完整對帳單用。

匯出是**新到舊**排序，所以「比最後一筆更新」的那一段才是完整的，更舊的部分不能用來判斷
「對帳單沒有」—— 否則會把真實存在的交易判成私帳多記而刪掉。要補的話，把查詢日期切小段，
讓每段結果都少於上限，再逐段匯出。

### 覆蓋率

哪幾段已經用對帳單核對過、哪幾段還沒有，以及還沒結案的個案，**都存在 Supabase 的
`klfan_recon_coverage`**，不寫進這裡 —— `AGENTS.md` 規定金融數字與個資不進 repo。
接手前先查一次，不要假設全部都驗過：

```sql
select broker, asset_class, kind, period_start, period_end, status,
       method, matched_rows, ledger_rows, open_items, note
from klfan_recon_coverage
order by status, broker, period_start;
```

一列 = 一個（券商 × 資產類別 × 買賣/股息 × 期間）區段。`status` 只有三種：
`verified`（逐筆雙向配對全中）／`partial`（改過但無法宣告全驗）／`unverified`。
**`matched_rows` 是配對得上的筆數**，`status='verified'` 時必須等於 `ledger_rows`。

改完覆蓋率一定要跑這個收尾檢查 —— 每一列私帳都要被涵蓋**剛好一次**，不重不漏：

```sql
select (select sum(ledger_rows) from klfan_recon_coverage) = 
       (select count(*) from klfan_transactions where stock_key not like '%-wife');
```

**`klfan_transactions.note` 不能拿來反推覆蓋率。** note 只記「改過什麼」，驗過而沒問題的
列不會留 note —— 2026-09-22 就是因為這樣，把「國泰台幣股息還缺幾筆」講錯過一次。
表建立前的歷史狀態無法回溯，一律以這張表為準。

沒有對帳單涵蓋的期間**維持原狀並標記待驗**。只有 Yahoo 股價區間可以用來抓「不可能的成交價」跟
漏掉的拆股，但它抓不到漏單，不能當成已核對。

## Supabase Production 防呆

Production migration history 與 `supabase/migrations/` 尚未完成 reconciliation，禁止直接執行
`supabase db push`。先讀 `supabase/README.md`，核對 production history；schema 提案只能放
`supabase/proposals/`，不得建立可執行 migration。
